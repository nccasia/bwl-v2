export interface MezonProfile {
  id: string;
  userName: string;
  email: string;
  avatar?: string;
  isFirstLogin: boolean;
  role: string;
}

export interface UserProfile {
  id: string;
  userName: string;
  email: string;
  avatar?: string;
  isFirstLogin: boolean;
  role: string;
}